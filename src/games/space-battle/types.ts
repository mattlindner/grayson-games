/**
 * @module types
 *
 * Shared type definitions used across the application.
 */

/**
 * Selectable player character.
 *
 * - `"grayson"` — Blue spaceship (default).
 * - `"quinn"`   — Pink unicorn spaceship.
 */
export type Character = "grayson" | "quinn";
